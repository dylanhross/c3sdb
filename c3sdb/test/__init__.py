"""
    c3sdb/test/__init__.py

    Dylan Ross (dylan.ross@pnnl.gov)

    Sub-package with unit tests for the parent c3sdb package. Uses python's built
    in `unittest` framework. Structure of test sub-package mirrors that of the 
    parent package (e.g. the module c3sdb/test/ml/data.py has unit tests for the
    c3sdb/ml/data.py module).
"""